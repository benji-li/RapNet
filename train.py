import torch
import torch.nn as nn
import torch.optim as optim
import numpy as np
import dataprep
import model

def fb_props(rnn,optimizer,criterion,inp,target,hidden):
    pass

def training_loop(rnn,batch_size,optimizer,criterion,n_epochs):
    pass